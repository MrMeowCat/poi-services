spring:
  application:
    name: poi_auth
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL}
      fail-fast: true
      retry:
        initial-interval: 5000
        max-interval: 5000
        max-attempts: 10
